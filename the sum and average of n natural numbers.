#include<stdio.h>
int main()
{
    int i,n;
    int sum;
    float avg;
    printf("\n enter the number :");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    
    sum =sum+i;
    avg = sum/n;
    printf("\n The sum of first n natural numbers is %d ",sum);
    printf("\n The average of first n natural numbers is %f ",avg);
    
    
    
}

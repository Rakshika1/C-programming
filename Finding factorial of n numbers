#include<stdio.h>
int main()
{
    int i;
    int fact=1;
    int num;
    printf("Enter the number:");
    scanf("%d",&num);
    if (num == 0)
        fact=1;
    else{
        for(i=1;i<=num;i++)
        fact=fact*i;
    }
    printf("\n Factorial of %d is %d ",num,fact);
    
    
    
}
